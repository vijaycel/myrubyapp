language: ruby
dist: bionic

rvm: 2.6.6

services:
  - redis

env:
  global:
    - PATH=/snap/bin:$PATH

cache: bundler

bundler_args: --without development --jobs 3 --retry 3 --deployment

before_install: gem install bundler
